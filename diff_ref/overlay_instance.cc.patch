--- /home/qed/Projects/py/goo-engine-linux-wrapper/diff_ref/overlay_instance.cc.from	2026-08-15 22:04:34.652398778 -0400
+++ /home/qed/Projects/py/goo-engine-linux-wrapper/diff_ref/overlay_instance.cc.to	2026-08-15 22:04:34.654564863 -0400
@@ -73,7 +73,12 @@
                                          BKE_scene_uses_blender_workbench(state.scene);
     const bool viewport_uses_eevee = STREQ(
         ED_view3d_engine_type(state.scene, state.v3d->shading.type)->idname,
-        RE_engine_id_BLENDER_EEVEE);
+        RE_engine_id_BLENDER_EEVEE) ||
+                                    STREQ(
+                                        ED_view3d_engine_type(state.scene,
+                                                               state.v3d->shading.type)
+                                            ->idname,
+                                        RE_engine_id_BLENDER_GOO_ENGINE);
     const bool use_resolution_scaling = BKE_render_preview_pixel_size(&state.scene->r) != 1;
     /* Only workbench ensures the depth buffer is matching overlays.
      * Force depth prepass for other render engines.
